name: Ventura Production Standard

on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:

permissions:
  contents: read
  security-events: write

jobs:
  standard:
    uses: venturalabs-ai/ventura.build/.github/workflows/repo-standard.yml@9016ac64be395739d8d64b497ee14c1f8431ceaf
